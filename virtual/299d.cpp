#include <string>
#include <iostream>
#include <stdio.h>
#include <queue>
#include <unordered_set>
#include <vector>
#include <iterator>
#include <math.h>
#include <algorithm>
#include <functional>
#include <cassert>

using namespace std;
typedef long long ll;


int main()
{
    // 最初がゼロで，最後が１なので，どこかで必ず 0から１に変わるはず
    // ということはにぶたんをインタラクティブにしていけばいいということはわかった
    // ただその実装が難しそうでマジで萎えている
    // いつもみたいに，ライブラリを使った煮豚んじゃどうしてもうまくいかないので，
    // スクラッチな感じでにぶたんを実装する能力が必要となるというわけ．
    // そしてそれは俺にとってはあまりにハードルの高い話

    // これはまあ普通に binary_search でやっぱりソロシイもよう
    // マジでさ，これからは焦らずゆっくりやっていくことにしましょう
    // 一日5問ペースを守っていくということで
    // それでも十分勉強にはなるはず，というか，今まで流石にテキトーにやりすぎてた疑惑は大いにありますよね
    // こんな感じでメモを残しながら解いていきましょうff

    int n, m; cin>>n>>m;

    
}